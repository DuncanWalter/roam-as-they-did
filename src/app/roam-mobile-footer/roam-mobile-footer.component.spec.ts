/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { RoamMobileFooterComponent } from './roam-mobile-footer.component';

describe('Component: RoamMobileFooter', () => {
  it('should create an instance', () => {
    let component = new RoamMobileFooterComponent();
    expect(component).toBeTruthy();
  });
});
